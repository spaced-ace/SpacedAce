package pages

import "spaced-ace/views/forms"
import "spaced-ace/models/request"

templ CreateNewQuizPage(viewModel CreateNewQuizPageViewModel) {
    <main class="flex h-full w-full justify-center p-6">
        @forms.CreateQuizForm(
            viewModel.Values
            viewModel.Errors,
        )
    </main>
}